# Frontend Environment Configuration

# API Configuration
# Use /api for proxy routing in development, or direct URL for production
NEXT_PUBLIC_API_URL=/api

# Backend Configuration (for development proxy)
BACKEND_URL=http://localhost:3001

# Environment Settings
NODE_ENV=development

# Optional: Analytics and Tracking (for future use)
# NEXT_PUBLIC_GA_TRACKING_ID=
# NEXT_PUBLIC_ANALYTICS_ENABLED=false

# Optional: Feature Flags (for future use)
# NEXT_PUBLIC_AI_FEATURES_ENABLED=true
# NEXT_PUBLIC_AUTOMATION_ENABLED=true