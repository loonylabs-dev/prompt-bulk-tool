{
  "name": "prompt-bulk-tool",
  "version": "1.0.0",
  "description": "Webapp für Bulk-Prompt-Erstellung und automatisierte Ausführung",
  "private": true,
  "workspaces": [
    "backend",
    "frontend",
    "automation",
    "shared"
  ],
  "scripts": {
    "dev": "concurrently --kill-others \"npm run dev -w backend\" \"npm run dev -w frontend\"",
    "build": "npm run build -w shared && npm run build -w backend && npm run build -w frontend",
    "start": "npm run start -w backend",
    "test": "npm run test --workspaces",
    "lint": "npm run lint --workspaces",
    "type-check": "npm run type-check --workspaces",
    "clean": "rimraf node_modules backend/node_modules frontend/node_modules automation/node_modules shared/node_modules",
    "setup": "npm install && npm run build -w shared"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "dependencies": {
    "caniuse-lite": "^1.0.30001739"
  }
}
